{
  "name": "simple-web-server",
  "version": "1.0.0",
  "description": "Simple web server for security scanning demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "4.16.0",
    "lodash": "4.17.4"
  }
}
